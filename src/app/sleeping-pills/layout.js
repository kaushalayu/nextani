import { generatePageMetadata } from '../../lib/seo-metadata'

export const metadata = generatePageMetadata(
  'Buy Sleeping Pills & Sleep Aids Online',
  'Shop quality sleep aids at Painomed — trusted options to help you fall asleep faster and stay asleep longer.',
  '/sleeping-pills'
)

export default function Layout({ children }) {
  return children
}
