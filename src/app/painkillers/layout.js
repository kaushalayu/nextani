import { generatePageMetadata } from '../../lib/seo-metadata'

export const metadata = generatePageMetadata(
  'Buy Painkillers Online',
  'Shop painkillers and pain relief medicines at Painomed — effective solutions for various types of discomfort with discreet delivery.',
  '/painkillers'
)

export default function Layout({ children }) {
  return children
}
